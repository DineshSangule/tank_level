import { OrderByOnlinePipe } from './order-by-online.pipe';

describe('OrderByOnlinePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByOnlinePipe();
    expect(pipe).toBeTruthy();
  });
});
